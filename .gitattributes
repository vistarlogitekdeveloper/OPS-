# Shell scripts must use LF or they fail on Linux (Render build).
*.sh text eol=lf
tool/render-build.sh text eol=lf

# Cloudflare Pages reads _redirects literally; keep it LF.
web/_redirects text eol=lf
